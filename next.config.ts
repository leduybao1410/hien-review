import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'localhost', '127.0.0.1', 'hienreview.vn'],
  },
};

export default nextConfig;
